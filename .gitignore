# Build output
/pulse
/dist/

# Frontend tooling. The UI is a Vite+Vue app in frontend/; its build output
# (frontend/dist/) is committed so the Go binary can embed it.
frontend/node_modules/

# Go
*.test
*.out
